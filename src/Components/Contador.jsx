import {userState} from 'react'

function Contador() {
    const [contador, setcontador] = userState(0)
    function somando(
        <div>
            <h2>{somando}</h2>
            <button onClick= {somando} >aqui</button>
        </div>
    )
}

export default Contador